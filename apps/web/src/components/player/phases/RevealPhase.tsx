import LoadingState from "../../shared/LoadingState";

export default function RevealPhase() {
  return (
    <LoadingState
      message="Reveal phase - coming soon"
      icon="clock"
    />
  );
}
