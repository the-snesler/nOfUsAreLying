import LoadingState from "../../shared/LoadingState";

export default function LeaderboardPhase() {
  return (
    <LoadingState
      message="Leaderboard phase - coming soon"
      icon="clock"
    />
  );
}
